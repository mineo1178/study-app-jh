import { battleEnergyState } from '../../rpg/battleUiLogic.js';
export default function EnemyCard({ enemy, profile, onStart, starting }) { const energy = battleEnergyState(profile, enemy); return <article className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><h3 className="font-black text-slate-800">{enemy.name}</h3><div className="mt-3 text-xs font-bold text-slate-600">HP {enemy.maxHp}<br/>消費 Energy {enemy.energyCost}<br/>勝利 EXP +{enemy.expReward}</div><p className={`mt-3 text-xs font-bold ${energy.sufficient ? 'text-emerald-600' : 'text-rose-600'}`}>Battle Energy {energy.energy} / 必要{energy.required}{energy.sufficient ? '' : ' ・ Energyが足りません'}</p><button type="button" disabled={!energy.sufficient || starting} onClick={() => onStart?.(enemy)} className="mt-4 w-full rounded-xl bg-rose-600 py-3 text-sm font-black text-white disabled:bg-slate-300">{starting ? '開始中…' : '戦う'}</button></article>; }
