import { describe, expect, it } from 'vitest'; import { BOSS_CATALOG } from './bossCatalog.js'; import { MATERIAL_KEYS } from './rewardConfig.js';
describe('boss catalog', () => { it('uses valid stats, elements and rewards', () => { Object.values(BOSS_CATALOG).forEach((boss) => { expect(boss.maxHp).toBeGreaterThan(0); expect(boss.attack).toBeGreaterThan(0); expect(boss.energyCost).toBeGreaterThan(0); expect(boss.expReward).toBeGreaterThan(0); expect(boss.weaknesses.some((id) => boss.resistances.includes(id))).toBe(false); expect(boss.firstClearReward.gold).toBeGreaterThanOrEqual(0); Object.entries(boss.firstClearReward.materials).forEach(([key, value]) => { expect(MATERIAL_KEYS).toContain(key); expect(value).toBeGreaterThanOrEqual(0); }); }); }); });
